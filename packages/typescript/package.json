{
  "name": "@gropyus/digital-factory-grpc",
  "version": "1.0.0",
  "description": "Digital Factory gRPC for TypeScript",
  "repository": {
    "type": "git",
    "url": "https://dev.azure.com/gropyus/mes/_git/digital-factory-grpc"
  },
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "clean": "rm -rf ./src && rm -rf ./dist",
    "build": "npm run clean && mkdir -p ./src && ./bin/generate.sh && tsc && cp package.json dist/package.json"
  },
  "author": "GROPYUS",
  "license": "UNLICENSED",
  "devDependencies": {
    "@grpc/grpc-js": "^1.6.12",
    "google-proto-files": "^3.0.2",
    "google-protobuf": "^3.21.0",
    "grpc-tools": "^1.11.2",
    "ts-proto": "^1.125.0",
    "typescript": "^4.8.3"
  },
  "peerDependencies": {
    "@grpc/grpc-js": "^1.6.12"
  }
}
