{
  "name": "moodn-grpc-api",
  "version": "1.0.0",
  "description": "moodn gRPC for TypeScript",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "clean": "rm -rf ./src && rm -rf ./dist",
    "build": "npm run clean && mkdir -p ./src && ./bin/generate.sh && tsc && cp package.json dist/package.json"
  },
  "author": "MOODN",
  "license": "MIT",
  "devDependencies": {
    "@grpc/grpc-js": "^1.7.1",
    "google-proto-files": "^3.0.2",
    "google-protobuf": "^3.21.0",
    "grpc-tools": "^1.11.2",
    "ts-proto": "^1.125.0",
    "typescript": "^4.8.3"
  }
}
